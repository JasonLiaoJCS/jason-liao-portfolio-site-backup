# 廖致翔學術申請網站｜完整使用、架構與維護說明書

> 文件版本：2026-08-24  
> 本機程式基準：`4839b7c`（`Complete final portfolio quality audit`）  
> 正式網站：<https://jason-liao-academic-portfolio.jasonliaock26.chatgpt.site>  
> 本機專案：`C:\Users\User\Desktop\學術申請用網站\jason-liao-portfolio-site`

這份文件同時是三種說明書：

1. **訪客使用說明**：如何瀏覽、搜尋、切換語言、查看完整成績、比較專案、播放影片、放大圖片與保存想重看的頁面。
2. **網站邏輯說明**：首頁為何這樣排序、各頁如何互相跳轉、什麼是 landing page、detail page、related work、完整索引與受邀資料。
3. **站主維護手冊**：本機資料在哪裡、哪些檔案是來源、哪些是自動生成、如何預覽、測試、備份、復原與重新發布。

本文件以目前本機程式碼為準。它不是網站上的行銷文案，而是供本人長期保存、理解與交接的操作文件。

---

## 目錄

1. [三十秒掌握整個網站](#一三十秒掌握整個網站)
2. [最建議的瀏覽方式](#二最建議的瀏覽方式)
3. [全站導覽與跳轉機制](#三全站導覽與跳轉機制)
4. [搜尋、Explore、隨機探索與已儲存頁面](#四搜尋explore隨機探索與已儲存頁面)
5. [首頁每一區的用途與操作](#五首頁每一區的用途與操作)
6. [主要分頁與內容層級](#六主要分頁與內容層級)
7. [一般集合頁與詳情頁的共同結構](#七一般集合頁與詳情頁的共同結構)
8. [完整修課與成績紀錄](#八完整修課與成績紀錄)
9. [專案比較、能力地圖、時間軸、作品星圖與影片作品集](#九專案比較能力地圖時間軸作品星圖與影片作品集)
10. [圖片、PDF、影片、自傳、CV、聯絡與受邀資料](#十圖片pdf影片自傳cv聯絡與受邀資料)
11. [手機版、鍵盤、無障礙與動畫規則](#十一手機版鍵盤無障礙與動畫規則)
12. [完整資訊架構與雙語網址規則](#十二完整資訊架構與雙語網址規則)
13. [內容、媒體與程式的技術架構](#十三內容媒體與程式的技術架構)
14. [站主日常維護流程](#十四站主日常維護流程)
15. [建置、測試與品質檢查](#十五建置測試與品質檢查)
16. [發布、備份與災難復原](#十六發布備份與災難復原)
17. [隱私、安全與外部服務邊界](#十七隱私安全與外部服務邊界)
18. [常見問題與故障排除](#十八常見問題與故障排除)
19. [發布前完整檢查表](#十九發布前完整檢查表)
20. [快速鍵與功能速查表](#二十快速鍵與功能速查表)

---

## 一、三十秒掌握整個網站

這個網站的核心不是「把所有資料堆在一起」，而是提供四條彼此相通的閱讀路徑：

```text
首頁先建立整體印象
├─ 研究：目前研究問題、方法、限制與證據
├─ 專案：工程系統、個人職責、關鍵決策與驗證
├─ 學術：成績、完整修課、數學與力學基礎、課程作品
├─ 領導／寫作／About：合作、教學、溝通與個人脈絡
└─ Archive：一次查看全部 75 項作品與經歷

任何頁面
├─ 主導覽：回到主要主題
├─ 搜尋：直接找特定內容
├─ Explore：依主題看策展路徑
├─ 隨機探索：全站等機會抽取一頁
└─ 延伸閱讀：從目前內容前往有直接關聯的下一頁
```

截至本文件版本，網站共有：

| 項目 | 數量 | 說明 |
|---|---:|---|
| 英文 canonical 路徑 | 90 | 英文使用無前綴網址 |
| 可索引雙語網址 | 180 | 每條英文頁各有一條 `/zh` 繁中頁 |
| 核心／集合頁 | 15 | 首頁、Research、Projects、Academics、Archive 等 |
| 作品與經歷內容頁 | 75 | 可由完整索引抵達 |
| 公開圖片 | 73 | 每張通常另有 AVIF、WebP 與 fallback 版本 |
| 公開 PDF／文件 | 44 | 含報告、簡報、證明、CV、修課紀錄 |
| 影片 | 12 | 本機保存海報圖，按播放後載入 YouTube |
| 完整自傳 | 2 | 繁中原文與逐段英文版本 |
| 修課狀態列 | 78 | 八學期 76 筆修課，加 2 筆免修 |

受邀資料頁 `/trusted` 與 `/zh/trusted` 是額外的動態頁，不屬於上述 90 條公開內容路徑，也不進入 sitemap、搜尋或隨機探索。

---

## 二、最建議的瀏覽方式

### 2.1 研究所審查者：八至十二分鐘

建議順序：

1. 首頁 Hero：先理解研究方向、雙主修與數學輔系背景。
2. 首頁「學業表現一覽」：快速看 GPA、A+ 學分與核心課程。
3. RedRHex：看目前研究問題、診斷方法與已驗證／進行中的界線。
4. Projects：從 Aero Carrier、Jarvis、LKAS 理解工程整合能力。
5. Academics：看代表課程，再進完整修課與成績紀錄。
6. CV：下載兩頁學術履歷。
7. 時間充裕時，再看完整自傳、Leadership 或 Archive。

### 2.2 實驗室教授或技術合作者

建議順序：

1. Research → RedRHex。
2. 由頁內目錄查看問題、限制、角色、轉折、驗證與後續。
3. 開啟完整研究報告或播放測試影片。
4. 由「延伸閱讀」前往 Numerical Analysis、Intermediate Dynamics。
5. Projects → 使用「專案比較」並列看問題、個人角色、驗證與下一步。
6. Contact → 寄送具體研究或合作詢問。

### 2.3 想完整了解個人經歷的訪客

建議順序：

1. 首頁。
2. About 與 Personal。
3. Writing 的兩篇完整自傳。
4. 首頁學術與工程時間軸。
5. Archive 完整索引。

### 2.4 沒有特定目的、想自由探索的訪客

使用左下角 **Explore** 或 **Surprise me／隨機探索**：

- Explore 適合依主題開始。
- Surprise Me 適合任意發現一頁。
- 看到想重看的頁面，可在 Explore 的「已儲存頁面」保存。

---

## 三、全站導覽與跳轉機制

### 3.1 桌面版頂部導覽

視窗寬度大於 1300px 時，頂部固定導覽列顯示：

| 控制 | 作用 |
|---|---|
| JL Logo／Jason Liao | 回首頁 |
| Research／研究 | 進入研究集合頁 |
| Projects／專案 | 進入工程專案集合頁 |
| Academics／學術 | 進入學術、課程與成績集合頁 |
| Leadership／領導與服務 | 進入帶隊、教學、志工與合作經歷 |
| Writing／寫作 | 進入自傳、技術與教學寫作 |
| About／關於我 | 進入學術背景與個人方向 |
| 放大鏡 | 開啟全站搜尋 |
| EN／中文 | 切換同一內容的另一語言 |
| CV | 直接下載公開兩頁 PDF |
| Contact／聯絡我 | 進入聯絡頁 |

頂部導覽是 `sticky`：頁面向下捲動後仍可使用。最上方另有一條細進度線，表示目前整頁的捲動百分比；它是視覺提示，不是按鈕。

### 3.2 左下角 Explore／Surprise Me

桌面版每個正常公開頁面左下角都有：

- **Explore／探索**：開啟主題導覽與已儲存頁面。
- **Surprise me／隨機探索**：直接抽取另一個公開頁面。

在 1301–1420px 的較窄桌面視窗，這組控制會縮成圖示，以避免壓到正文；功能與無障礙名稱仍保留。

### 3.3 手機與平板導覽

視窗寬度在 1300px 以下時，桌面主選單與左下角 dock 會收起，改由右上角選單按鈕開啟。選單內含：

1. 探索導覽
2. 隨機探索
3. 研究
4. 專案
5. 學術
6. 領導與服務
7. 寫作
8. 關於我
9. 完整索引
10. 聯絡我
11. 下載 CV
12. 搜尋作品集

在 720px 以下，導覽列會隱藏文字品牌，只保留 Logo、語言切換與選單按鈕，為正文保留寬度。

手機選單本身可以在視窗內捲動；它不鎖死背景頁面的 root scroller。點擊選單外側時，第一次點擊只負責關閉選單，不會同時誤觸下面的按鈕。

### 3.4 語言切換

英文是 canonical 語言：

- 英文首頁：`/`
- 英文頁：`/research/redrhex`

繁體中文使用 `/zh`：

- 繁中首頁：`/zh`
- 繁中頁：`/zh/research/redrhex`

語言切換不是回首頁，而是切換**同一頁內容**。一般左鍵操作也會保留 query string 與 hash，例如：

```text
/projects/aero-carrier#evaluation
        ↓ 中文
/zh/projects/aero-carrier#evaluation
```

若用中鍵或 `Ctrl/Cmd` 開新分頁，瀏覽器直接使用控制本身的基本 `href`，額外 query/hash 不一定保留。

### 3.5 頁尾導覽

每頁底部都有完整補充導覽：

- Explore：Research、Projects、Academics、Leadership。
- Profile：About、Writing、Personal、Updates。
- Contact & documents：CV、Contact、Email、GitHub、受邀資料。
- 完整作品索引。

如果在內頁迷路，頁尾是最穩定的回復路徑。

### 3.6 返回頁首

桌面／平板在向下捲動超過 `max(720px, 80% 視窗高度)` 後，右下角出現返回頁首按鈕。它會：

1. 將焦點放回 `#main-content`。
2. 一般模式平滑回到頁首。
3. 使用者偏好減少動畫時，立即跳回頁首。

720px 以下的手機版不顯示這顆懸浮按鈕，以免遮住內容或瀏覽器工具列。

---

## 四、搜尋、Explore、隨機探索與已儲存頁面

這四種工具解決不同問題：

| 工具 | 最適合的情況 |
|---|---|
| Search | 已知道關鍵字、課名、專案名或主題 |
| Explore | 不知道精確頁名，但知道想看研究、系統、學術等方向 |
| Surprise Me | 想隨機發現全站任一頁 |
| Saved pages | 想建立自己的短清單，稍後依序重看或分享 |

### 4.1 全站搜尋

開啟方式：

- 桌面版頂部放大鏡。
- 手機選單中的搜尋。
- Windows/Linux：`Ctrl+K`。
- macOS：`Cmd+K`。
- 焦點不在輸入欄時按 `/`。

可選搜尋範圍：

1. 全部
2. 研究
3. 工程專案
4. 學術與課程
5. 寫作
6. 經歷與生活

沒有輸入文字時，搜尋會顯示最多八個優先入口；有輸入文字時，最多顯示 12 筆結果，但狀態文字會顯示完整符合筆數。

搜尋會同時處理：

- 當前語言標題與摘要。
- 另一語言標題與摘要。
- 分類與 tags。
- 大小寫、Unicode 全半形、重音、標點與符號差異。
- 完整修課頁的所有課號、雙語課名與領域。

所有輸入詞都必須出現在該筆內容中。例如輸入 `RedRHex control`，結果必須同時含有兩個詞。排名大致為：標題完全相符／開頭相符 > 標題包含 > tags／分類 > 摘要。

鍵盤操作：

- `↑`／`↓`：循環選取結果。
- `Home`／`End`：第一筆／最後一筆。
- `Enter`：開啟選取頁。
- `Escape`：關閉。

搜尋視窗會鎖住背景捲動、將鍵盤焦點留在對話框內，關閉後回到原本的觸發按鈕。`/trusted` 不會出現在搜尋中。

### 4.2 Explore 主題導覽

Explore 有兩個分頁：

1. **主題導覽**
2. **已儲存頁面**

主題導覽共有五個 lens，每個 lens 有三個策展入口：

| Lens | 三個代表入口 |
|---|---|
| 研究方向 | RedRHex、幾何覆蓋最佳化、數值分析 |
| 工程系統 | Aero Carrier、Jarvis、LKAS |
| 學術深度 | 數值分析、中等動力學、工程數學 |
| 合作與執行 | Aero Carrier、TRML 隊長、國際學生文件協助 |
| 寫作 | 〈從迷惘到自洽〉、工程數學筆記、教學與數學溝通 |

每次開啟 Explore，會先回到「主題導覽」，並依目前頁面推測最合理的 lens：

- Projects → 工程系統。
- Academics → 學術深度。
- Writing／About／Personal → 寫作。
- Leadership、TRML 或學生支持內容 → 合作與執行。
- 其他 → 研究方向。

這只是預設起點，不會限制你切換其他主題。

### 4.3 Surprise Me／隨機探索

隨機探索的規則是：

1. 候選池來自全站公開搜尋路徑。
2. `/trusted` 不在候選池。
3. 只要還有其他候選頁，就排除目前頁，避免按下後原地不動。
4. 每一個 canonical 公開頁面有相同抽取機會，不受 Explore lens、分類大小或近期瀏覽影響。
5. 使用瀏覽器的加密亂數，並以 rejection sampling 避免整數取餘數造成的微小偏差。
6. 不刻意排除歷史頁面，因此不同次點擊仍可能再次抽到曾看過的頁。

英文與繁中共用同一組 canonical 候選；抽中後會以目前語言開啟。

### 4.4 已儲存頁面

在 Explore 切到「已儲存頁面」後，可以：

- 儲存／取消儲存目前頁。
- 分享目前頁。
- 複製一份含標題、分類、摘要與直接網址的編號清單。
- 依序前往「下一個已儲存頁面」。
- 移除單一頁面。
- 清除全部已儲存頁面。
- 從最近瀏覽清單快速返回或保存頁面。

儲存規則：

- 最多 12 頁。
- 新存的頁面排在最前面。
- 英文與繁中網址會合併為同一 canonical 頁，不會重複占位。
- 只接受目前仍存在的公開路徑。
- 最近瀏覽最多保留六頁。
- 清除已儲存頁面沒有二次確認與復原。

資料只放在目前瀏覽器的 `localStorage`：

- 不會上傳伺服器。
- 沒有帳號同步。
- 換瀏覽器、換裝置、清除網站資料後不會保留。
- 若瀏覽器禁止儲存，功能仍可操作，但重新整理後可能消失。

分享時優先使用手機／瀏覽器的原生分享面板；不支援時改為複製網址。取消原生分享面板不會顯示錯誤。

### 4.5 Explore 與 Search 的互斥

Search 和 Explore 不會同時開啟。這是刻意的：

- 降低兩個 modal 互相覆蓋的風險。
- 避免鍵盤焦點跑到背景控制。
- 避免手機版出現未觸發卻殘留框線或透明面板。

---

## 五、首頁每一區的用途與操作

首頁採「先實質證據，再互動探索，再補充人格與完整索引」的順序。共 11 個主要區塊。

### 5.1 Hero：身份與網站入口

內容：

- 研究與工程主張。
- 國立臺灣大學機械工程與土木工程雙主修、數學輔系。
- Robotics、Mechanics、Applied Mathematics。
- 個人照片與目前 RedRHex 研究提示。

三個主要 CTA：

- View research／查看研究。
- Download CV／下載 CV。
- Explore projects／探索專案。

六步工作方法列：

1. Define／定義
2. Model／建模
3. Integrate／整合
4. Verify／驗證
5. Document／紀錄
6. Handoff／交接

桌面版下方金色導引線只是節奏動畫；手機版改成多列後會停用橫向移動線，避免在二維排列中出現不合理的掃動。

### 5.2 學業表現一覽

這是首頁最優先的實質證據區，呈現：

- 機械工程主修 GPA。
- 機械系定必修 GPA。
- A+ 計分學分。
- 數學、核心力學與計算課程表現。
- 臺大書卷獎。
- 整體 GPA。

明顯的「查看全部課程與成績」按鈕會進入 `/academics/course-record`；GPA 採計與換算說明則進入 `/experience/ntu-gpa-a-plus-record`。兩者不同：前者是完整逐學期紀錄，後者是計算範圍、方法與換算依據。

### 5.3 目前研究：RedRHex

用來快速說明：

- 正在處理的研究問題。
- 模擬、步態診斷與真機測試。
- 已驗證結果、進行中分支與限制的界線。

按鈕進入完整 RedRHex 頁，可閱讀報告、圖片與影片。

### 5.4 代表工程系統

首頁優先呈現三個有實體成果與影像的專案：

- Jarvis 多模態家庭中樞。
- Aero Carrier 四旋翼搬運系統。
- LKAS 視覺車道置中系統。

整張卡片可點擊；卡片不是只展示封面，而是進入完整問題、角色、轉折、驗證與文件。

### 5.5 代表課程成果與 Process Lab

代表課程：

- 數值分析。
- 中等動力學。
- 工程數學。

同區的 Process Lab 把六步工作方法連到真實案例：

| 步驟 | 案例 |
|---|---|
| 定義 | RedRHex：從高 reward 重新問「是否真的走對」 |
| 建模 | LKAS：物理模型、感知與控制鏈 |
| 整合 | Inventor：總組合、介面與動畫 |
| 驗證 | Numerical Analysis：誤差、殘差、收斂與交叉檢查 |
| 紀錄 | Aero Carrier：16 版決策與 140 頁團隊報告 |
| 交接 | RedRHex：基準、實驗分支與下一步 |

點選任一步驟，右側／下方內容會替換；沒有自動輪播。每個 panel 末端有「查看相關成果」。

### 5.6 四項學術與工程能力

四張摘要卡整理：

- 學術準備。
- 研究判斷。
- 系統整合。
- 團隊合作。

它們是首頁摘要，不會取代後面的互動能力地圖與原始作品頁。

### 5.7 能力與作品地圖

五個能力面向：

1. 研究判斷
2. 理論基礎
3. 驗證與查核
4. 系統整合
5. 領導與溝通

點擊圓環上的能力，指針與內容 panel 會同步切換；每個面向提供三條證據連結。手機版不強迫保留圓環，而改為清楚的兩欄按鈕，避免節點重疊。

### 5.8 影像作品集

首頁整合 12 支影片，可依：

- 全部影片（12）
- 研究與機器人（4）
- 工程系統（6）
- 課程作品（2）

分類、切換影片或「換一部影片」只會換選取項目，不會自動播放；需要再按播放鍵。每支影片另有「查看專案內容」。

### 5.9 學術與工程時間軸

時間軸不是只挑六件事，而是將 21 筆代表紀錄整理成五個時期：

1. 2016–2019：數學基礎。
2. 2019–2022：研究、教學與帶隊。
3. 2022–2024：臺大跨域基礎。
4. 2024–2025：模型進入實體系統。
5. 2025–至今：研究、整合與實作。

每筆都是正常直接連結；這一區沒有自動播放、選取狀態或輪播。最後可進完整作品索引。

### 5.10 領導、教學與研究之外

用來補足純成績與技術無法呈現的面向：

- TRML 帶隊。
- Inventor 團隊整合。
- 同儕複習活動。
- 科學推廣。
- 國際學生支持。

可前往 Leadership 或 About。

### 5.11 作品集地圖

首頁最後提供六個長期入口：

- 研究。
- 工程專案。
- 學術與課程。
- 領導與服務。
- 寫作。
- 互動星圖與完整索引。

它的功能是讓讀到頁尾的訪客重新選擇方向，不需要回頂部。

---

## 六、主要分頁與內容層級

### 6.1 主要頁面速查

| 頁面 | URL | 主要用途 |
|---|---|---|
| 首頁 | `/`、`/zh` | 整體定位、學業亮點、研究、專案與互動入口 |
| Research | `/research` | 目前研究與早期數學研究 |
| Projects | `/projects` | 五個工程專案與專案比較 |
| Academics | `/academics` | 成績、課程、數學力學基礎與榮譽 |
| Course Record | `/academics/course-record` | 八學期全部課程、成績、篩選與雙語 PDF |
| Honors | `/academics/honors` | 12 項學術與競賽榮譽 |
| Leadership | `/leadership` | 領導、教學、服務與協作 |
| Writing | `/writing` | 兩篇自傳、技術寫作與教學寫作 |
| Teaching | `/writing/teaching` | 教學材料、同儕複習與科學推廣 |
| About | `/about` | 學業、研究背景與專業方向 |
| Personal | `/personal` | 音樂、棒球、旅行、服務與個人經歷 |
| Updates | `/updates` | 21 筆人工策展的重要里程碑 |
| Archive | `/archive` | 全部 75 筆內容與互動星圖 |
| CV | `/cv` | 學業摘要與兩頁 CV 開啟／下載 |
| Contact | `/contact` | Email 與聯絡前快速入口 |
| Trusted | `/trusted` | 受邀者的保護式資料索引；不公開索引 |

### 6.2 Research

共三筆可見內容：

1. RedRHex（精選）。
2. 加入臺大仿生機器人實驗室 BioRoLa。
3. 幾何覆蓋最佳化。

研究集合頁先讓訪客看到具體作品，再顯示較抽象的頁面摘要。

### 6.3 Projects

五個專案：

1. Aero Carrier。
2. Jarvis。
3. LKAS。
4. Inventor 小球循環系統整合。
5. Polar Arm。

前三項為精選，後兩項緊接在「其他工程專案」，之後才進入專案比較工具，避免互動功能壓過真正作品。

### 6.4 Academics

Academics 的 28 筆集合內容為：

- 15 門正式課程成果。
- 1 筆 GPA 採計與換算說明。
- 12 項榮譽。

分組：

1. 學業紀錄與計算方法。
2. 數學基礎。
3. 力學、動力學與控制。
4. 計算與實驗。
5. 設計與製造。
6. 榮譽與學術肯定。

精選五項：Numerical Analysis、Intermediate Dynamics、Engineering Mathematics、Mechanical Laboratory II、Linear Algebra & FFT。

### 6.5 Leadership

集合頁同時收納 `leadership` 與 `teaching_service` 類型，共 16 筆。首頁／集合頁優先展示鐵馬週、土木營、機械宿營與同儕複習，其他再依大學、高中與更早階段整理。

### 6.6 Writing

共七筆：

- 兩篇完整自傳。
- 兩筆技術／研究寫作：Mathematica 曲線座標基底、GitHub 工程與研究紀錄。
- 三筆教學／數學溝通：1,600+ 頁工程數學筆記、泰勒展開影片、2021 數學科普寫作。

`/writing/teaching` 是跨類型集合，也納入同儕複習、課後輔導、科學社教學與三場科學營，共九筆；它不是複製一份新內容。

### 6.7 About 與 Personal

About 偏向：

- 教育背景。
- 目前研究。
- 專業方向。
- 影響專業發展的重要經歷。

Personal 偏向：

- 棒球。
- 音樂演出。
- 旅行與跨文化經驗。
- 服務與個人關係。

兩頁都會保留照片，但照片標題依真實性質稱為「個人照片」，不把個人生活照誤稱為「成果影像」。

### 6.8 Updates

Updates 不是所有資料依日期自動排序，而是 21 筆人工策展里程碑。它適合快速查看近期成果與重要轉折，不等於完整履歷；完整性應以 Archive 為準。

### 6.9 Archive

Archive 是唯一保證從單一頁面抵達全部 75 筆作品與經歷的介面。它先顯示完整索引，再顯示互動星圖，避免華麗功能把完整內容壓到很後面。

---

## 七、一般集合頁與詳情頁的共同結構

### 7.1 集合頁（landing page）

一般集合頁順序：

1. Hero：分類、標題、摘要、聯絡按鈕。
2. 本頁導覽。
3. 精選內容。
4. 完整集合索引。
5. 本頁自有照片／圖表（若有）。
6. 重點摘要卡。
7. 專屬互動元件（例如 Projects 的專案比較）。

以下頁面採 evidence-first，也就是先放作品，再放抽象摘要：

- Research
- Projects
- Academics
- Honors
- Leadership
- Writing
- Writing／Teaching
- Updates

About 與 Personal 則較偏 profile-first：精選 → 照片 → 背景摘要 → 完整索引。

同一集合內的一般排序：

1. 大學。
2. 其他／跨時期。
3. 高中。
4. 國中與更早。
5. 同階段內依內容 tier（S → A → B → C），再依年份由新到舊。

### 7.2 詳情頁（detail page）

一般內容詳情頁順序：

1. Breadcrumb：首頁 → 邏輯父集合 → 目前頁。
2. Hero：分類、成熟度、標題、摘要、返回父集合。
3. 代表圖片或 PDF 預覽。
4. 若有文件，提供「開啟完整文件」。
5. 本頁目錄。
6. 敘事段落。
7. 在相關段落穿插照片、圖表與影片。
8. 完整事件／課程詳解。
9. 文件卡：開啟或下載 PDF。
10. 延伸閱讀。

詳情頁的本頁目錄會用 IntersectionObserver 追蹤目前章節。桌面為側邊直式目錄；1050px 以下改為橫向 sticky chip rail。進度條表示「目前在第幾章」，不是精確的像素捲動百分比。

### 7.3 六段研究／專案案例結構

主要研究與專案案例通常使用：

1. Thesis／核心主張。
2. Question & constraints／問題與限制。
3. Role & system／我的角色與系統。
4. Turning point／關鍵轉折。
5. Evaluation／驗證結果。
6. Next／限制、後續與意義。

`Next` 是內容段落，不是自動跳到下一頁。

### 7.4 課程頁結構

15 門正式課程頁先用三個高階段落：

1. 理論問題。
2. 方法與驗證。
3. 主要成果。

再附完整課程詳解與相關 PDF／圖片。

### 7.5 Breadcrumb 父層不是單看 URL

許多歷史資料使用穩定的 `/experience/*` 網址，但 breadcrumb 會按語意回到最自然的集合：

- 研究 → Research。
- 專案 → Projects。
- 課程 → Academics。
- 榮譽 → Honors。
- 領導／教學服務 → Leadership。
- 個人 → Personal。
- 教育背景 → About。
- GPA 說明 → Course Record。
- 無其他適合分類時 → Updates。

因此 `/experience/*` 是穩定內容網址，不代表網站有一個名為 Experience 的主要分頁。

### 7.6 延伸閱讀

一般詳情頁沒有固定線性的「上一篇／下一篇」。其規則是：

1. 讀取該內容的 `relatedRoutes`。
2. 移除已經出現在 breadcrumb 的父集合。
3. 第一筆關聯內容顯示成大型「延伸閱讀」。
4. 其餘最多顯示五張相關卡。
5. 沒有關聯內容時整段不顯示。

這讓下一步取決於內容關係，而不是機械式依日期前後切換。

真正具有「下一個」的功能包括：

- 自傳：固定前往另一篇自傳。
- Archive 星圖：上一個／下一個節點。
- Explore：下一個已儲存頁面。

---

## 八、完整修課與成績紀錄

正式路徑：

- 英文：`/academics/course-record`
- 繁中：`/zh/academics/course-record`

### 8.1 主要入口

- 首頁「學業表現一覽」。
- Academics 的學業成果摘要。
- CV 頁的學業摘要。
- 能力與作品地圖的理論基礎。
- 全站搜尋。
- GPA 採計說明的 breadcrumb／related link。

### 8.2 頁面順序

1. Hero 與完整性統計。
2. 學業亮點。
3. 課程查詢。
4. 逐學期完整紀錄。
5. 課程領域摘要。
6. 資料來源、計算方式與雙語 PDF。

頁首提供兩顆明顯按鈕：

- 下載雙語 PDF。
- 查看全部課程（跳至完整紀錄）。

本頁導覽是正常流中的橫條，不會黏在螢幕上一直跟隨。

### 8.3 目前數字

| 指標 | 數值 |
|---|---:|
| 學期 | 8 |
| 實際修課紀錄 | 76 |
| 免修紀錄 | 2 |
| 總狀態列 | 78 |
| 計分課程 | 67 |
| 計分學分 | 156 |
| 通過制課程 | 5 |
| 通過制學分 | 7 |
| 停修紀錄 | 4 |
| 停修申請學分 | 12 |
| 已取得學分 | 163 |
| A+ 紀錄 | 46 |
| A+ 計分學分 | 110 |

主要學業摘要：

- 機械工程主修 GPA：4.19／4.30；逐科換算 3.96／4.00。
- 機械系定必修 GPA：4.21／4.30；逐科換算 3.98／4.00。
- 整體 GPA：4.13／4.30；逐科換算 3.92／4.00。
- A+：110／156 計分學分，70.5%。
- 數學、核心力學與計算：20 筆、50 學分，全數 A+。

這些 GPA 是依學分加權、自行逐科計算；不是臺大成績單直接核發的獨立欄位。網站與 PDF 都保留這項界線。

### 8.4 課程查詢操作

可搜尋：

- 中文課名。
- 英文課名。
- 課號。
- 課程領域。
- 學期標籤。

十一個領域：

1. 數學與解析。
2. 力學與動力學。
3. 計算、控制與電子。
4. 熱流、能源與傳輸。
5. 設計、製造與實驗。
6. 土木基礎。
7. 物理與化學。
8. 研究訓練。
9. 語言、人文與社會。
10. 團隊實踐與體育。
11. 服務與學習導入。

額外篩選：

- 僅顯示 A+。
- 只顯示有作品連結。
- 重設全部。

不同篩選可同時使用。搜尋結果顯示符合筆數與「所代表的學分」；若結果包含停修，這個學分是紀錄中的名目學分，不等於已取得學分。

課程查詢不會刪除或改寫下方完整紀錄；它只是額外的跨學期查找工具。

### 8.5 逐學期完整紀錄

- 最新學期排最前面。
- 最新兩學期預設展開。
- 其他學期與免修預設收合。
- 多個學期可以同時展開。
- 滑鼠、觸控、`Enter`、`Space` 均可操作原生 `<details>`。

每列顯示：

- 當前語言課名。
- 另一語言課名。
- 領域與備註。
- 課號。
- 學分。
- 成績／通過／停修／免修。
- 對應作品連結（若有）。

### 8.6 PDF

公開雙語 PDF：

`/documents/Jason_Liao_Public_Course_Record_Through_Spring_2026.pdf`

可在瀏覽器開啟或直接下載，內容包含學業亮點、計算說明與逐學期完整紀錄。

---

## 九、專案比較、能力地圖、時間軸、作品星圖與影片作品集

### 9.1 Projects 的專案比較

可比較五個專案：

- Aero Carrier。
- Jarvis。
- LKAS。
- Inventor 系統整合。
- Polar Arm。

操作：

1. 選擇兩至三個專案。
2. 選擇比較面向。
3. 在並列結果中閱讀差異。
4. 點「查看專案詳情」直接跳至該專案相同章節。

預設建議組合為 Aero Carrier + Jarvis。最少保留兩項、最多三項；若違反限制，按鈕仍可點，但系統會用狀態訊息說明為何沒有變更。

五個比較面向：

1. 問題與限制。
2. 我的角色。
3. 關鍵轉折。
4. 驗證結果。
5. 限制與後續。

選取順序決定欄位順序。「建議組合」會同時重設專案與比較面向。狀態只存在目前頁面，重新整理後回到預設。

### 9.2 能力與作品地圖

這是首頁的五能力互動元件。桌面使用等分圓環與指針；窄螢幕改為按鈕網格。它的目的不是測驗或計分，而是把抽象能力連到三個真實證據頁。

鍵盤可用四個方向鍵循環切換，`Home`／`End` 到第一／最後一項。

### 9.3 Process Lab

這是首頁的六步工作方法元件。它沒有自動輪播，避免訪客還沒讀完就切走。桌面為左右工作區；840px 以下改為 3×2，720px 以下 2×3，380px 以下單欄。

### 9.4 時間軸

首頁時間軸為靜態五時期、21 筆紀錄。它不保存選取狀態、不自動播放，也不使用 carousel；每筆直接前往內容頁。760px 以下改為單欄。

### 9.5 Archive 互動作品星圖

星圖位於完整索引之後，共 14 個精選節點：

- 研究 2。
- 工程系統 2。
- 學術 3。
- 合作與成長 7。

篩選：全部、研究、工程系統、學術、合作與成長。空分類不顯示。

線條只來自內容資料中明確記錄的 `relatedRoutes`，不是依字詞相似度猜測。選擇分類後：

- 該分類節點是主要節點。
- 與目前節點直接相連的跨分類節點仍保留作為脈絡。
- 目前關係線保持可見。

可用：

- 點節點。
- 點相關內容 chip。
- 上一個／下一個。
- 隨機選擇節點。
- 開啟完整頁面。

鍵盤方向鍵會依畫面幾何位置找指定方向上最近的節點，不是單純依編號；`Home`／`End` 到目前篩選的第一／最後一項。

760px 以下，星圖改成兩欄清單並隱藏連線；430px 以下改成單欄。這是刻意的 responsive 替代，不是節點消失。

操作注意：Archive 上方的「依類型快速前往」會跳至該類型第一次出現的位置，但若它位於仍收合的原生 `<details>` 階段中，瀏覽器不一定主動展開；此時先展開相應的「高中階段」或「更早期」即可。

### 9.6 Portfolio Cinema

影片清單切換只更新海報與選取狀態，不會自動播放。按播放後才建立 YouTube iframe。

操作：

1. 選分類。
2. 在影片清單選一支。
3. 按海報中央播放。
4. 可在本頁看、到 YouTube 開啟，或進相應作品頁。
5. 關閉按鈕會移除播放器並回到海報。

一次只保留一個 active iframe；開始另一支影片會讓前一支回到海報。載入超過 12 秒或失敗會顯示重試畫面。

桌面為影片 + 直式清單；980px 以下清單移到影片下方成水平 scroll-snap rail；760px 以下分類為兩欄，430px 以下單欄。

---

## 十、圖片、PDF、影片、自傳、CV、聯絡與受邀資料

### 10.1 圖片放大

詳情頁的 Hero 圖片與 gallery 圖片可點擊放大。放大視窗：

- 保留完整比例，不用 `cover` 裁掉內容。
- `X`、背景、`Escape` 均可關閉。
- 開啟時焦點進入 Close，鍵盤焦點不會跑到背景。
- 關閉後焦點回到原圖片。
- 背景捲動鎖住，並補償 scrollbar 寬度避免畫面跳動。

目前不提供 zoom、pan、上一張／下一張、幻燈片或圖片下載按鈕。首頁一般照片、Archive 縮圖與卡片封面不是 lightbox；點卡片會進內容頁。

### 10.2 PDF 與文件

一般詳情頁可能有兩種入口：

- Hero 的「開啟完整文件」。
- 文件卡的「開啟」與「下載 PDF」。

文件卡顯示標題、頁數、檔案大小、PDF 類型與第一頁預覽。PDF Hero 的預覽圖本身通常不是按鈕；應使用相鄰文字按鈕。

### 10.3 影片

- 本機網站先顯示本地 poster。
- 按播放後才連到 `youtube-nocookie.com`。
- 這降低初始載入與未互動前的第三方請求。
- 播放器另有 YouTube 外部連結與無 JavaScript fallback。
- 影片播放器沒有全站 `Escape` 關閉；請用可見的 `X`。

### 10.4 兩篇完整自傳

Writing 內有：

1. 〈從迷惘到自洽〉：六章，繁中約 16 分鐘、英文約 24 分鐘。
2. 《格物翔者·自序》：兩章，繁中約 2 分鐘、英文約 4 分鐘。

自傳頁有專屬閱讀版面：返回 Writing、文章資訊、章節目錄、完整正文、About、所有 Writing、另一篇自傳。

繁中頁保留原文全文；英文頁逐段對應，未做摘要式刪節。閱讀時間是依當前語言估算，不是固定寫死。

### 10.5 CV

CV 頁：

- `/cv`
- `/zh/cv`

內容：

1. 下載或在瀏覽器開啟。
2. 學業表現摘要。
3. 兩頁學術履歷介紹。

公開 PDF：

`/documents/Chih-Hsiang_Liao_Public_Academic_CV_2026-08.pdf`

Header、首頁與頁尾的 CV 快捷多半直接指向 PDF；`/cv` 則多一層學業脈絡。

### 10.6 Contact

聯絡頁提供：

- Email：`JasonLiaohyh9815@gmail.com`
- 預填主旨：`Portfolio inquiry — Jason Liao`
- Research、Projects 與 CV 快速入口。

點 Email 會呼叫裝置預設郵件程式。若裝置沒有設定郵件程式，可手動複製 Email。

### 10.7 受邀資料 `/trusted`

受邀資料是受密碼保護的「審閱入口」，不是把所有敏感原檔直接放上網的私有雲端硬碟。

未登入時：

- 顯示密碼欄、顯示／隱藏密碼。
- 說明八小時工作階段與資料邊界。
- 錯誤密碼、嘗試過多與服務未設定有不同訊息。

登入後：

- 顯示學業摘要。
- 依研究、升學、工程與合作分類導覽。
- 連到現有公開 CV、課程、報告與影片。
- 對正式證明或第三方資料，提供預填 Email 索取。

它不在 Search、Surprise Me、Saved pages 驗證池、sitemap 或公開索引中。`noindex`／robots 是降低搜尋引擎發現，不是密碼安全本身；真正存取控制由伺服器 session 驗證處理。

---

## 十一、手機版、鍵盤、無障礙與動畫規則

### 11.1 主要 responsive 節點

| 寬度 | 主要變化 |
|---|---|
| >1420px | 完整桌面導覽與完整 Explore dock |
| 1301–1420px | 桌面導覽保留，Explore dock 縮成圖示 |
| ≤1300px | 改用手機／平板選單；隱藏左下 dock |
| ≤1050px | 詳情頁目錄改為橫向 rail；部分雙欄開始堆疊 |
| ≤980px | 影片清單移到播放器下方 |
| ≤840px | Process Lab 變 3×2；自傳目錄移入正文前 |
| ≤760px | 複雜圓環／星圖改為清單；滑鼠 3D 效果停用 |
| ≤720px | 手機排版、品牌文字隱藏、回頁首按鈕隱藏 |
| ≤430px | 多數互動控制單欄或橫向 scroll-snap |
| ≤380px | Process Lab 六步改為單欄 |

### 11.2 鍵盤快捷

| 快捷鍵 | 功能 |
|---|---|
| `Ctrl+K`／`Cmd+K` | 搜尋 |
| `/` | 搜尋（焦點不在可編輯欄位時） |
| `Alt+E` | Explore |
| `Alt+S` | Surprise Me |
| `Escape` | 關閉搜尋、Explore、手機選單或圖片 lightbox |
| `↑`／`↓` | 搜尋結果、部分 tab／queue 切換 |
| `←`／`→` | tab、能力、比較面向或節點切換 |
| `Home`／`End` | 到目前控制組第一／最後一項 |
| `Enter` | 開啟搜尋結果或按鈕 |
| `Space` | 原生 `<details>` 等控制 |

快捷鍵會避開輸入框、textarea、select、可編輯內容與中文輸入法 composition，不會搶走正在輸入的按鍵。

### 11.3 Focus 與 dialog

- 首次按 Tab 會出現 Skip to content。
- Search、Explore、lightbox 使用 modal semantics。
- Tab／Shift+Tab 留在對話框。
- 關閉後回到觸發來源；若來源已不存在，改找可見替代按鈕。
- 手機選單用 inert／visibility 控制關閉狀態，避免看不見的按鈕仍可被 Tab 到。
- `aria-current` 標示目前主導覽或章節。
- live region 宣告搜尋筆數、保存結果、比較限制、課程結果、星圖選取與影片載入。

### 11.4 滑鼠與卡片效果

只有同時符合下列條件才啟用細緻滑鼠互動：

- 視窗至少 761px。
- 裝置具有 hover 與 fine pointer。
- 使用者沒有要求 reduced motion。
- 沒有開 forced colors。

效果包括：

- 區塊進場 reveal。
- 指標 aura／trace。
- Hero 輕微視差。
- 卡片深度傾斜。
- 按鈕小幅 magnetic offset。
- 連結依滑鼠方向微移。

效果幅度被限制，且 Search、Explore、表單、dialog、lightbox、能力地圖、專案比較、星圖、Cinema 等複雜控制被排除，避免 hover transform 破壞點擊位置。

所有互動在捲動、pointer down/cancel/leave、視窗失焦、resize、頁面切換或分頁隱藏時重設，避免殘留傾斜、框線或透明面板。

### 11.5 Reduced motion、forced colors、print

`prefers-reduced-motion`：

- 關閉 smooth scroll、view transition、掃描線、圓環 sweep、指標效果與 reveal 隱藏。
- 所有內容直接可見。
- 不會因停用動畫而留下透明內容。

Forced colors：

- 關閉裝飾線與 pointer motion。
- 為控制和區塊加入明確邊界。
- 移除影片海報 veil，確保高對比可見。

Print：

- 改成淺色文件版面。
- 隱藏導覽、搜尋、Explore、影片、gallery 與純互動控制。
- 專案比較／星圖只印目前 active panel，不會把所有可能狀態全部展開。

---

## 十二、完整資訊架構與雙語網址規則

### 12.1 內容類型

75 筆非核心內容：

| 類型 | 數量 |
|---|---:|
| 研究 | 3 |
| 工程專案 | 5 |
| 課程成果 | 16（15 門正式課程 + GPA 說明） |
| 榮譽 | 12 |
| 領導 | 7 |
| 教學與服務 | 9 |
| 一般經歷 | 9 |
| 個人 | 7 |
| 寫作 | 7（含 2 篇自傳） |

時期：大學 46、其他／跨時期 1、高中 21、國中與更早 7。

### 12.2 URL 家族

| 家族 | canonical 數量 | 備註 |
|---|---:|---|
| `/` | 1 | 首頁 |
| `/research` | 3 | landing + 2 個 research detail；加入 BioRoLa 使用 `/experience/*` |
| `/projects` | 6 | landing + 5 個專案 |
| `/academics` | 18 | landing、修課、榮譽、15 門課程 |
| `/writing` | 9 | landing、teaching、2 自傳、5 寫作頁 |
| `/experience/*` | 46 | 穩定的學歷、獎項、領導、服務與生活詳情 URL |
| 其他核心單頁 | 7 | Leadership、About、Contact、CV、Archive、Updates、Personal |

### 12.3 舊網址永久轉址

保留 query string：

- `/en` → `/`
- `/en/*` → `/*`
- `/experience` → `/updates`
- `/zh/experience` → `/zh/updates`

舊 coursework aliases：

| 舊路徑 | 新 canonical |
|---|---|
| `/academics/coursework/engineering-mathematics` | `/academics/engineering-mathematics` |
| `/academics/coursework/intermediate-dynamics` | `/academics/intermediate-dynamics` |
| `/academics/coursework/linear-algebra-and-applications` | `/academics/linear-algebra-fft` |
| `/academics/coursework/mechanical-engineering-laboratory-ii` | `/academics/mechanical-laboratory-ii` |
| `/academics/coursework/numerical-analysis` | `/academics/numerical-analysis` |
| `/academics/coursework/physical-model-design-lab` | `/academics/coursework/physical-model-design-laboratory` |
| `/academics/coursework/python-programming` | `/academics/coursework/computer-programming-in-python` |

以上也各有 `/zh/...` 對照。

### 12.4 SEO 與搜尋引擎

- 每個可索引內容有英文與 `zh-Hant` alternate。
- 英文為 `x-default`。
- Sitemap 共 180 條語言 URL。
- 首頁優先度最高；主要 landing 與 Archive 次高；內容頁再次之。
- `/trusted`、`/zh/trusted`、`/api/trusted/*` 由 robots 排除，且回應帶 noindex。
- metadata、Open Graph 與結構化資料由同一內容模型生成，避免頁面文案與搜尋摘要分離。

---

## 十三、內容、媒體與程式的技術架構

### 13.1 技術堆疊

- React 19.2.6。
- Next.js App Router 相容程式，由 Vinext 1.0.0-beta.2 編譯。
- Vite 8.0.13。
- TypeScript 5.9.3，strict、noEmit。
- Tailwind CSS 4.2.1 + `app/globals.css` 大量自訂樣式。
- Cloudflare Worker + `@cloudflare/vite-plugin` + Wrangler。
- Node.js 需求：22.13.0 以上。

Drizzle／D1 套件目前只保留空 scaffold：

- `.openai/hosting.json` 的 `d1` 與 `r2` 均為 `null`。
- 網站核心不依賴資料庫或物件儲存。
- `examples/d1/` 不是正式網站路由。
- `app/chatgpt-auth.ts` 也是未使用 scaffold；受邀登入實作在 `lib/trusted-auth.ts`。

### 13.2 資料流

```text
專案上一層的編號資料夾
├─ 權威 Markdown／成績彙整
├─ asset-manifest.v1.json
└─ 原始圖片、PDF、影片
        ↓ 生成與公開資產腳本
lib/*.json + public/* 公開衍生物
        ↓
lib/content-data.ts + lib/page-runtime.ts + lib/videos.ts
        ↓
英文 root routes + 繁中 /zh routes
        ↓
PageView / HomePage / SiteFrame 等元件
        ↓
Vinext + Vite build
        ↓
dist/client + dist/server + dist/.openai
        ↓
Cloudflare Worker + Sites
```

### 13.3 主要目錄

| 路徑 | 作用 |
|---|---|
| `app/` | 英文／繁中 routes、redirect、metadata、sitemap、robots、trusted API |
| `components/` | 首頁、頁面 renderer、導覽、搜尋、lightbox、互動元件 |
| `lib/content-data.ts` | 中央雙語內容、路由、分類、摘要、關聯 |
| `lib/page-runtime.ts` | 集合、分組、搜尋、媒體、代表資產與統計 |
| `lib/content-navigation.ts` | Breadcrumb 邏輯父層 |
| `lib/event-public-details.json` | 51 筆一般經歷的生成式雙語完整內容 |
| `lib/coursework-public-details.json` | 15 門課程的生成式雙語完整內容 |
| `lib/course-record.json` | 八學期訪客安全版修課紀錄 |
| `lib/academic-record.json` | GPA 與學業摘要數字，人工維護 |
| `lib/media-manifest.json` | 117 個公開媒體邏輯紀錄與 release metadata |
| `lib/media-exceptions.json` | 八個 withheld 圖片來源與安全替代規則 |
| `lib/videos.ts` | 12 支 YouTube 影片與本地 poster mapping |
| `public/` | 直接對外公開的實體檔案 |
| `scripts/` | 內容、媒體、PDF 生成與 redaction 工具 |
| `worker/index.ts` | 正式 request handler、安全 headers、URL 正規化 |
| `tests/` | 八個 Node 測試檔，共 94 個 test declaration |
| `.openai/hosting.json` | Sites 專案綁定；不得隨意改 project id |
| `.dev.vars` | 本機秘密，Git ignore，不進 dist |
| `dist/` | 建置輸出；可重新生成，不在 Git |

### 13.4 公開媒體模型

`public/` 目前有 437 個實體檔案、約 212.9 MB：

- 73 張邏輯圖片 × AVIF／WebP／fallback = 219 檔。
- 44 份 PDF。
- 44 組 PDF 第一頁預覽 × 3 = 132 檔。
- 12 組影片 poster × 3 = 36 檔。
- 其餘 6 個 icon／social／root 資產。

`lib/media-manifest.json` 有 117 個邏輯項目，分布在 44 條內容路徑。它不是公開 URL 下可直接下載的 JSON，而是建置時供伺服器選資產使用。

媒體腳本會：

- 依 SHA-256 去重與驗證來源。
- 修正 EXIF orientation。
- 正確解碼 HEIC。
- 將長邊限制在 2560px。
- 產生 metadata-clean 的 AVIF、WebP 與 JPEG／PNG。
- 複製核准 PDF、整理 metadata、產生第一頁預覽。
- 掃描可抽取 PDF 文字中的身分證號、電話、秘密、密碼與本機路徑。
- 對已遮蔽／withheld 衍生物固定來源與輸出 hash。

限制：自動文字掃描不會 OCR PDF 內嵌圖片。任何新證書、截圖或掃描 PDF 仍需人工逐頁看過。

### 13.5 內容優先權

公開 route 的解決順序：

1. 固定核心頁。
2. 完整自傳。
3. 展開的 case study。
4. 其餘課程。
5. 其餘事件。

12 個 expanded case study 會在相同 canonical route 上取代較短事件紀錄，不會產生重複 URL。

---

## 十四、站主日常維護流程

### 14.1 最安全的原則

1. **先改來源，後跑 generator**；不要先手改生成 JSON，再期待下次重建保留。
2. **不要直接把原始敏感資料複製到 `public/`**。
3. **不要手改 hash 來通過測試**；hash 不符代表來源或公開衍生物真的改了。
4. **每次改動都跑 lint + test**。
5. **發布前先看桌面與手機**。
6. **`.dev.vars` 不可加入 Git、壓縮公開包或傳給第三人**。
7. **`.openai/hosting.json` 的 `project_id` 不可因資料夾改名而重建**。

### 14.2 只改一般頁面文字

常見檔案：

- `lib/content-data.ts`：標題、摘要、卡片文案、section、relatedRoutes。
- `lib/event-public-details.json`：由來源與 generator 生成的一般經歷完整內容。
- `lib/coursework-public-details.json`：由來源與 generator 生成的課程完整內容。
- `components/HomePage.tsx`：首頁專屬順序與互動文案。
- `components/PageView.tsx`：共用 landing/detail、CV、Contact 文案。

建議流程：

1. 判斷文字是「資料內容」還是「介面文案」。
2. 兩語一起改，保留自然對應，不要求逐字直譯。
3. 若 JSON 是由 generator 產生，回到上一層來源或 generator 的 editorial replacement 修改。
4. 檢查卡片標題、Hero 標題、搜尋結果與 metadata 是否一致。
5. 跑 lint/test。

### 14.3 新增或修改一般內容頁

至少要確認：

- 唯一 `id`。
- `kind`。
- canonical `route`。
- 英／中 title、eyebrow、summary、card、detail。
- sections。
- `relatedRoutes`。
- category。
- maturity。
- tier、featured、dateLabel、tags、stage（如適用）。

新增後還要檢查：

- 是否應出現在某 landing collection。
- Breadcrumb 父層是否正確。
- Search scope 是否正確。
- Archive 統計與 count assertion 是否需有意更新。
- 是否要放入首頁、Explore lens、Updates 或星圖。
- 是否兩語 route 都能 render。
- 404、sitemap 與 structured data 是否正常。

### 14.4 調整頁面關聯

修改 `relatedRoutes` 時，同時思考：

- 它會影響詳情頁延伸閱讀。
- 它可能影響 breadcrumb 父層。
- 若兩個路徑都在 Archive 星圖 blueprint，會影響關聯線。
- 順序決定第一張大型延伸閱讀卡與後面小卡順序。

不要只因兩頁有相同關鍵字就連結；應有可說明的內容關係。

### 14.5 更新照片、PDF 或其他公開媒體

來源 manifest：

`..\00_先看這裡_網站資料說明\manifests\asset-manifest.v1.json`

一般流程：

```powershell
node ..\00_先看這裡_網站資料說明\manifests\_maintenance\build-asset-manifest.mjs

python -m pip install -r scripts/requirements-media.txt
python scripts/build_public_assets.py
```

之後必須：

1. 查看 `lib/media-manifest.json` 的 route、title、alt、caption、display order。
2. 人工檢查每張新圖片與 PDF 每一頁的隱私。
3. 確認頁面代表圖沒有被不適合的圖片取代。
4. 確認直式、橫式、混合比例在桌面與手機正常。
5. 確認 `public/` 沒有 unmanaged extra file。
6. 跑完整測試。

若新來源資料夾命名不符合 `route_for()` 已知規則，資產可能落到 `/about`；此時要補 route mapping，不可只在頁面上遮掩。

### 14.6 更新修課與成績

權威來源：

`..\03_學歷成績與學術證明\03-09_臺大GPA成績單與A+課程\廖致翔_完整學業成績彙整_截至114-2.md`

流程：

```powershell
node scripts/generate-course-record.mjs
```

然後人工核對：

- `lib/course-record.json`。
- `lib/academic-record.json`（此檔沒有 generator，必須人工同步 GPA 摘要）。
- 課程頁／相關作品 route。
- 中英文課名翻譯。
- 已取得、計分、通過、停修、免修、A+ 數量。

再生成 PDF 與媒體：

```powershell
python scripts/build_public_course_record_pdf.py
python scripts/build_public_assets.py
```

PDF builder 需要指定字體與 `pdftoppm`。若自動找不到字體，可設定 `COURSE_RECORD_FONT_DIR`。

### 14.7 更新課程或一般經歷完整內容

```powershell
node scripts/generate-coursework-public-details.mjs
node scripts/generate-event-public-details.mjs
```

生成後要看差異，不可假設 generator 一定保留人工語氣。重要正式文案應在權威來源或 generator 的 editorial replacement 中維護。

### 14.8 更新影片

修改 `lib/videos.ts`：

- YouTube ID。
- 英中標題與摘要。
- 分類 playlist。
- 對應 route。
- duration。
- 本地 poster 三格式路徑。
- presentation 位置（如有）。

檢查：

- 首頁分類數量。
- poster 是否存在。
- 點擊後能否播放。
- CSP 是否允許 `youtube-nocookie.com`。
- 「查看專案內容」是否正確。

影片原檔不會進部署；原檔要留在上一層本機來源樹。

### 14.9 更新 CV

目前固定公開路徑由 `lib/site-config.ts` 的 `cvPath` 指向：

`/documents/Chih-Hsiang_Liao_Public_Academic_CV_2026-08.pdf`

若只換內容且想保留所有舊連結，建議沿用同一路徑；若檔名改版，必須同步：

- `siteConfig.cvPath`。
- asset manifest／media manifest。
- `public/documents`。
- PDF preview。
- tests。
- 所有外部履歷連結或申請文件。

### 14.10 建議的完整生成順序

來源有變動時：

```powershell
node ..\00_先看這裡_網站資料說明\manifests\_maintenance\build-asset-manifest.mjs
node scripts/generate-course-record.mjs
node scripts/generate-coursework-public-details.mjs
node scripts/generate-event-public-details.mjs

python -m pip install -r scripts/requirements-media.txt
python scripts/build_public_course_record_pdf.py
python scripts/build_public_assets.py
```

不是每次小改字都要跑全部；只有相應來源改動時才跑相應 generator。跑之前先確認不會用舊來源覆蓋較新的正式文案。

---

## 十五、建置、測試與品質檢查

### 15.1 安裝與預覽

```powershell
Set-Location 'C:\Users\User\Desktop\學術申請用網站\jason-liao-portfolio-site'
npm ci
npm run dev
```

- `npm ci`：依 lockfile 重建相同依賴；日常復原優先用它，不用 `npm install`。
- `npm run dev`：啟動 Vinext 開發站。

### 15.2 指令

```powershell
npm run lint
npm run build
npm run start
npm test
```

| 指令 | 作用 |
|---|---|
| `npm run lint` | ESLint 檢查 app、components、lib、worker、tests、config |
| `npm run build` | 產生 `dist/` |
| `npm run start` | 執行已存在的 production build |
| `npm test` | 先 build，再執行 `tests/*.test.mjs` |

`npm test` 不包含 lint，所以正式發布前要分別執行 `npm run lint` 與 `npm test`。

### 15.3 目前自動測試覆蓋

八個測試檔、94 個 test declaration，涵蓋：

- 180 條雙語 sitemap URL。
- 404、redirect 與品牌頁。
- 75 筆 Archive 與內容數量。
- 雙語文案完整性。
- 媒體 hash、大小、資料夾完整性與 withheld hash。
- 修課 JSON、GPA、PDF 註冊與計算協調。
- Trusted login、origin、cookie tamper、form fallback、logout。
- Search、Saved pages、Surprise route。
- 鍵盤與 focus pattern。
- responsive CSS、reduced motion、圖片比例與元件結構。

限制：部分「視覺」與無障礙測試是 source/CSS assertion，不是真實瀏覽器像素比較；YouTube 與外部連結也不會每次 live 測試。因此自動測試通過後仍要人工看正式站。

### 15.4 人工 QA 裝置組合

本說明書建立後已在 2026-08-24 依本機程式基準實際執行：

- `npm run lint`：通過。
- `npm test`：production build 通過，94／94 tests 通過，0 failed、0 skipped、0 todo。

這證明目前資料、路徑、build 與既有自動檢查一致；它仍不能取代下列真實瀏覽器與裝置檢查。

至少檢查：

1. 桌面 1440px 以上。
2. 介於 1301–1420px 的窄桌面。
3. 1024px 平板。
4. 390–430px iPhone／Android。
5. 320–380px 極窄畫面。
6. 英文與繁中。
7. reduced motion。
8. 鍵盤-only。

每種至少走：首頁 → Search → Explore → detail → PDF／圖片 → Course Record → Projects compare → Archive → Contact。

---

## 十六、發布、備份與災難復原

### 16.1 Sites 綁定

`.openai/hosting.json` 目前只保存：

```json
{
  "project_id": "appgprj_6a7a57fea43c8191b3dcc1add80a852e",
  "d1": null,
  "r2": null
}
```

資料夾改名不會讓網站失效；真正重要的是：

- Git 專案根目錄仍正確。
- 相對路徑仍完整。
- `project_id` 未被誤換。
- `public/` 與 `lib/media-manifest.json` 一致。
- Sites runtime secrets 仍設定。
- build/test 通過。

網站沒有 `npm run deploy`。正式發布透過 Sites 控制面完成：建置、保存精確版本、部署、等待成功，再檢查正式 URL。不要把來源憑證寫入 Git remote URL 或設定檔。

### 16.2 發布前技術順序

```text
確認工作樹與變更
→ npm run lint
→ npm test（包含 production build）
→ 以 Sites 打包 dist + hosting metadata
→ 儲存版本
→ 部署至既有公開存取層級
→ 等待 succeeded
→ 正式站桌面／手機抽查
```

若只完成本機文件或其他不影響網站輸出的工作，不需要重新部署。

### 16.3 本機完整性

目前網站是 file-based：

- 程式碼、雙語文案、圖片、PDF、poster、metadata 都可由本機 Git source 重建。
- 沒有必要的遠端 D1／R2 資料。
- 12 支影片播放依賴 YouTube，但原始影片另保存在上一層本機來源。
- GitHub 與 GPA 參考來源是外部連結，不是網站核心 rendering 依賴。
- 字型使用系統 fallback；Course Record PDF 生成另需要本機字型。

如果未來加入 D1 或 R2，這個「只靠檔案即可完整復原」的結論會改變，必須新增資料庫與 object storage 備份。

### 16.4 D 槽版本備份

版本備份根目錄：

`D:\學術申請用網站_本機備份`

不同日期／commit 的子資料夾通常含：

- Git bundle。
- 已建置 deploy tar.gz。
- `hosting.json`。
- `RECOVERY.md`。

較完整的全機復原包另含：

- 完整來源樹。
- `.dev.vars`。
- source ZIP。
- Windows dependencies archive。
- deploy build。
- SHA-256 清單。

重要現況：本文件基準是 `4839b7c`，而目前可見的最新版本型 D 槽備份停在較早 commit；較新的小版本資料夾也不一定包含完整來源 ZIP、依賴封存與獨立 `SHA256SUMS.txt`。因此下一次大改前，應先製作一份以目前 HEAD 為準的完整復原包，並再做一份加密的離機備份。C、D 都在同一台電腦，無法防止整機遺失、火災或硬碟同時故障。

### 16.5 只用 Git bundle 復原

```powershell
git clone .\source-<commit>.bundle jason-liao-portfolio-site
Set-Location .\jason-liao-portfolio-site
git checkout main
npm ci

# 私下恢復 .dev.vars，並在 Sites 重新設定 runtime secrets
# 若網域改變，設定 SITE_URL 與 NEXT_PUBLIC_SITE_URL

npm run lint
npm test
```

Git bundle 足以重建既有公開網站，因為 437 個公開檔案都已追蹤。若要重新從原始照片、PDF、Markdown 生成內容，仍需要上一層完整來源樹與 authoritative manifest。

### 16.6 網域變更

建置前設定：

- `SITE_URL`
- `NEXT_PUBLIC_SITE_URL`

若要改永久 fallback，也要同步：

- `lib/site-config.ts` 的 `defaultUrl`。
- `lib/seo.ts` 的 `PUBLIC_SITE_ORIGIN`。
- `scripts/build_public_course_record_pdf.py` 的 fallback URL，或在正確環境變數下重建 PDF。

---

## 十七、隱私、安全與外部服務邊界

### 17.1 什麼一定是公開的

**任何放在 `public/` 的檔案，不論檔名或是否只從 `/trusted` 連結，都能由知道 URL 的人直接取得。**

因此：

- 敏感原始檔不可放入 `public/`。
- 要公開證明時，只放經人工檢查的遮蔽衍生版。
- `noindex` 不能代替真正的存取控制。

### 17.2 本機來源與公開衍生物

上一層 `99_敏感資料_禁止直接公開` 等資料夾保存原始敏感或第三方資料；它們不在網站 Git bundle 與部署輸出。公開版本由 manifest 明確選取，部分遮蔽檔以完整 hash 固定。

注意：「只存在本機」不等於「只有本人帳號可讀」。目前 C 槽部分來源與 `.dev.vars` 的 Windows ACL 仍可能允許本機較廣泛的使用者群組修改；這不會自動公開到網路，但若電腦有其他本機帳號或工具執行環境，應另行收緊 ACL。D 槽完整保護快照目前限制在本人、SYSTEM 與 Administrators。

### 17.3 Trusted authentication

- 伺服器以 constant-time digest 比對共享密碼。
- Session token 使用 HMAC-SHA-256 簽章。
- 有效八小時。
- Cookie：`__Host-`、HttpOnly、Secure、SameSite=Lax、Path=/。
- 驗證同源 Origin。
- Request body 上限 4 KiB。
- 五次失敗／十分鐘的 in-memory rate limit。
- `TRUSTED_SESSION_SECRET` 至少 32 UTF-8 bytes。

限制：

- Rate limit 只在當前 Worker memory instance，cold start 後可能重設。
- 使用單一共享密碼，沒有逐人 audit log。
- 更換 session secret 會讓所有舊 session 失效。
- 缺少或錯誤 secrets 時登入回 HTTP 503。

### 17.4 外部依賴

- YouTube：按播放後載入 `youtube-nocookie.com`。
- GitHub：外部專案連結。
- GPA 轉換：外部參考來源連結。
- Email：使用 `mailto:`。

網站沒有 analytics，也沒有把訪客互動寫入遠端應用資料庫。Local storage 只有最近／已儲存路徑。

---

## 十八、常見問題與故障排除

### 18.1 手機選單點了消失或留下框線

檢查：

- 關閉狀態是否同時具有 hidden visibility、pointer-events none 與 inert。
- 是否有舊 dev server 或快取載入到不同版本 CSS／JS。
- 捲動後 `.site-header`、mobile nav fixed panel 與 root scroller 是否仍在相同 stacking context。
- 先停止重複 dev server，再清理可重建的 Vinext/Vite/Miniflare cache，重新 build。

### 18.2 Search 找不到頁面

- 確認內容已進 `allContent`。
- 確認 route 不是 `localOnly` 或 `/trusted`。
- 檢查 title、card、category、tags 與另一語言內容。
- 課程資料要重新生成 `course-record.json`。

### 18.3 Surprise Me 看起來一直抽到同類

目前每頁同機率，但短時間仍可能隨機重複。它不記錄歷史，也不做分類配額。確認 `chooseSurpriseRoute()` 的輸入仍來自全站搜尋路徑，沒有被 Explore lens 替換。

### 18.4 新媒體跑到 About

新來源資料夾可能沒有對應 `route_for()` 規則。更新 mapping 後重建資產；不要在 About 頁暫時隱藏，因為 manifest route 才是真正來源。

### 18.5 媒體 hash 或 missing-file 測試失敗

- 比對 parent manifest 與原始 bytes。
- 確認檔案沒有在處理後又被手動覆寫。
- 不要手改 hash 讓測試綠燈。
- 重新跑 asset manifest 與 public assets builder。

### 18.6 `public/` 多出未管理檔案

測試會拒絕 documents、images、PDF previews 中的 unmanaged extra。把檔案納入 manifest 與流程，或移出 `public/`；不要只把它加進 ignore。

### 18.7 PDF builder 不能啟動

確認：

- Python requirements 已安裝。
- 六套所需 Noto／Georgia／Consolas 字體可找到。
- `pdftoppm` 可執行。
- 必要時設定 `COURSE_RECORD_FONT_DIR`。

### 18.8 Trusted login 503／403／429

- 503：兩個 `TRUSTED_*` 環境變數缺失或 secret 少於 32 bytes。
- 403：Origin／forwarded host 不一致。
- 429：依 `Retry-After` 等候；in-memory limiter 會隨 Worker memory 重設。

### 18.9 影片不播放

- 本地 poster 正常但播放器失敗：檢查 YouTube availability、video ID 與 CSP。
- 載入 12 秒後會進 retry 狀態。
- 確認 production CSP 的 `frame-src` 允許 `youtube-nocookie.com`。

### 18.10 語言切換到錯頁

- canonical route 應只儲存英文無前綴形式。
- 不要在 `content-data.ts` 把 `/zh` 或 `/en` 寫進 canonical route。
- 檢查 `localizePath()`、legacy redirect 與 query/hash preservation。

### 18.11 新內容讓 count assertion 失敗

這通常表示資料治理數量真的改了。應有意更新預期總數、Archive stats、sitemap 測試與文件，不要刪除 assertion。

### 18.12 改網域後 sitemap／PDF 還是舊網址

同時檢查 `SITE_URL`、`NEXT_PUBLIC_SITE_URL`、`site-config.ts`、`seo.ts` 與 course PDF builder fallback，然後重新生成 PDF 與 build。

---

## 十九、發布前完整檢查表

### 19.1 內容

- [ ] 中文與英文標題自然、正式、沒有 AI／建站語氣。
- [ ] 本人原文與自傳沒有被不當改寫。
- [ ] 數字、日期、角色、成熟度與限制和來源一致。
- [ ] 「我的角色」沒有把團隊成果誤寫為個人獨立完成。
- [ ] 個人照片、成果圖、圖表、證明與報告的標題性質正確。
- [ ] Related routes 合理且不重複。

### 19.2 路徑與功能

- [ ] 英文與繁中同頁切換正確。
- [ ] Header、mobile menu、footer 正常。
- [ ] Search 可開、可關、可鍵盤選取。
- [ ] Explore、Surprise Me、Saved pages 正常。
- [ ] Course Finder 所有篩選可組合、重設正常。
- [ ] Projects comparison 維持 2–3 項限制。
- [ ] Capability map 五等分、panel 不消失。
- [ ] Archive 完整索引在星圖前，75 筆可達。
- [ ] 星圖節點、線、panel、上一個／下一個正常。
- [ ] 所有 CTA、breadcrumb、anchor、related link 可到正確位置。

### 19.3 媒體

- [ ] 代表圖片選得合理。
- [ ] 圖片左右沒有異位、超出、壓字或強制裁掉重要內容。
- [ ] 直式／橫式混合 gallery 在桌面與手機正常。
- [ ] Lightbox 可開、可關、焦點可返回。
- [ ] PDF 開啟與下載都正常。
- [ ] 每份新 PDF 逐頁人工檢查 PII。
- [ ] 影片 poster、播放、關閉、YouTube 外部連結正常。

### 19.4 視覺與 responsive

- [ ] 1440+、1301–1420、1024、430、390、320–380px 無跑版。
- [ ] 文字沒有交疊、截斷或超出卡片。
- [ ] 金色邊框四周粗細一致。
- [ ] 漸層沒有突兀亮帶或斷邊。
- [ ] 多列手機控制沒有沿用桌面單向進度線。
- [ ] Logo 置中、比例協調、沒有黑邊。
- [ ] Hover／tilt 不改變真實點擊位置。
- [ ] reduced motion 下內容完整可見。

### 19.5 技術與發布

- [ ] `git status` 只包含本次預期變更。
- [ ] `npm run lint` 通過。
- [ ] `npm test` 通過。
- [ ] `public/` 與 media manifest 一致。
- [ ] `.dev.vars` 沒進 Git／部署包。
- [ ] `.openai/hosting.json` project id 未變。
- [ ] Sites secrets 仍存在。
- [ ] 部署狀態為 succeeded。
- [ ] 正式 URL 英文／繁中／手機抽查通過。
- [ ] 建立本次 commit 的復原包與 hash。

---

## 二十、快速鍵與功能速查表

### 訪客

| 想做的事 | 最快方法 |
|---|---|
| 找特定頁 | `Ctrl/Cmd+K` |
| 隨機看一頁 | `Alt+S` 或 Surprise Me |
| 依主題探索 | `Alt+E` 或 Explore |
| 收藏目前頁 | Explore → 已儲存頁面 → 儲存此頁 |
| 分享一組頁面 | Explore → 已儲存頁面 → 複製頁面清單 |
| 看全部內容 | Archive／完整索引 |
| 查課程 | Academics → 完整修課與成績 → 課程查詢 |
| 比較工程專案 | Projects → 專案比較 |
| 看全部影片 | 首頁 → 影像作品集 |
| 看完整圖片 | 詳情頁點圖片的放大按鈕 |
| 下載 CV | Header／首頁／Footer 的 CV |
| 聯絡 | Contact 或 Footer Email |

### 站主

| 想做的事 | 主要位置／指令 |
|---|---|
| 改雙語內容 | `lib/content-data.ts` 或相應權威來源／generator |
| 改首頁順序 | `components/HomePage.tsx` |
| 改共用詳情頁 | `components/PageView.tsx` |
| 新增媒體 | parent manifest → `build_public_assets.py` |
| 更新成績 | 權威成績 MD → `generate-course-record.mjs` → PDF/media build |
| 更新影片 | `lib/videos.ts` + local posters |
| 預覽 | `npm run dev` |
| Lint | `npm run lint` |
| Build + tests | `npm test` |
| 重建依賴 | `npm ci` |
| 完整內容入口 | `/archive` |
| Sites 綁定 | `.openai/hosting.json`（不可任意換 project id） |
| 本機復原說明 | `LOCAL_RECOVERY.md` |

---

## 結語：理解這個網站最重要的三個觀念

1. **它不是單一路徑履歷，而是互相連結的證據網路。**主導覽提供分類，Search 提供直接查找，Explore 提供策展路徑，related work 提供內容關係，Archive 提供完整性。
2. **華麗互動不能取代內容。**成績、研究、專案與課程先出現；比較、能力地圖、Cinema、時間軸與星圖負責幫助理解，而不是藏住實質成果。
3. **公開網站與本機來源是兩個明確層次。**`public/` 內一定公開；敏感原始資料留在上一層來源樹；網站可由本機 Git 與公開資產重建，但重新生成媒體與完整內容仍需 authoritative source tree。

若未來網站再新增功能，請同步更新本文件的版本基準、數量、操作方式、測試項目與復原流程，讓說明書始終能反映正式網站，而不是成為過時備忘錄。
